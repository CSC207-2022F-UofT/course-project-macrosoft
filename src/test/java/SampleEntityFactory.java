import entities.*;
import org.bson.types.ObjectId;

public class SampleEntityFactory {
    public Order getOrder(){
        return null;
//        return new Order(new ObjectId(), new Date());
    }
}
